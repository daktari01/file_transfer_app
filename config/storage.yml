test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: andela-media
  credentials: <%= ENV['GOOGLE_APPLICATION_CREDENTIALS'].as_json %>
  bucket: todo-pic-pjbelo
google_dev:
  service: GCS
  project: todo-pic
  credentials: <%= Rails.root.join("config/secrets/gallery-gcp.json") %>
  bucket: media-bucket-876
